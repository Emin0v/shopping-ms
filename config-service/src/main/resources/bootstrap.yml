
server:
  port: ${SERVER_PORT:8888}

spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/ms-config

  profiles:
    active: native